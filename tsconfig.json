// {
//   "compilerOptions": {
//     "target": "ES2022",
//     "module": "ES2022",
//     "moduleResolution": "node",
//     "allowJs": true,
//     "checkJs": true,
//     "noEmit": true,
//     "strict": true,
//     "esModuleInterop": true,
//     "lib": ["ES2022", "DOM"]
//   },
//   "include": ["js"]
// }

{
  "compilerOptions": {
    "target": "ES2022",                  // Указывает, до какой версии JavaScript будет транслироваться код (в данном случае ES2022).
    "module": "ES2022",                  // Указывает формат модулей, который будет использоваться (в данном случае ES2022-модули, аналогично ECMAScript модулям).
    "moduleResolution": "node",          // Определяет, как TypeScript будет искать модули (как в Node.js — через node_modules и index-файлы).
    "allowJs": true,                     // Разрешает компилятору обрабатывать .js файлы наряду с .ts.
    "checkJs": true,                     // Включает проверку типов и ошибок в JavaScript-файлах.
    "noEmit": true,                      // Отключает генерацию выходных файлов (TypeScript только проверяет код, но не генерирует .js).
    "strict": true,                      // Включает все строгие проверки типов (например, строгая проверка null и undefined).
    "esModuleInterop": true,             // Позволяет использовать `import` синтаксис с CommonJS модулями, улучшая совместимость.
    "lib": ["ES2022", "DOM"]             // Указывает стандартные библиотеки, которые будут использоваться (в данном случае ECMAScript 2022 и DOM API).
  },
  "include": ["js"]                      // Указывает, что папка "js" должна быть включена в компиляцию.
}
